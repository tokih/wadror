<p id="notice"><%= notice %></p>

<h2><%= @brewery.name %></h2>

<p><em>
Established in <%= @brewery.year %>
</em></p>

<p>Number of beers in the database: <%= "#{@brewery.beers.count}" %></p>

<p>
   <% @brewery.beers.each do |beer| %>
      <%= link_to "#{beer.name}", beer %>
   <% end %>
</p>

<% if @brewery.ratings.empty? %>
 <p>Brewery does not yet have any rated beers!</p>
<% else %>
 <p>Has <%= pluralize(@brewery.ratings.count,'rating','ratings') %>, average <%= "#{@brewery.average_rating}" %></p>
<% end %>

<%= link_to 'Delete', brewery_path(@brewery), method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to 'Edit', edit_brewery_path(@brewery) %> |
<%= link_to 'Back', breweries_path %>
<%= link_to 'List of beers', beers_path %>
